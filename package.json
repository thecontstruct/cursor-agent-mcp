{
  "name": "mcp-cursor-agent",
  "version": "1.1.0",
  "type": "module",
  "private": true,
  "description": "MCP wrapper server for cursor-agent CLI. Multi-tool: chat/edit/analyze/search/plan/raw + legacy run.",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.3",
    "zod": "^3.23.8"
  },
  "scripts": {
    "start": "node ./server.js",
    "test": "vitest run",
    "test:unit": "vitest run server.test.js",
    "test:integration": "vitest run server.integration.test.js",
    "test:e2e": "vitest run server.e2e.test.js",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "@vitest/ui": "^2.1.9",
    "vitest": "^2.1.9"
  }
}
